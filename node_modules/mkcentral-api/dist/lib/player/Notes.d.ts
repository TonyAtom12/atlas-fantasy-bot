import { Player } from "../Player";
export declare class Notes {
    /** Notes text */
    Notes: string;
    /** Player who edited the notes (optional) */
    EditedBy: Player | null;
    /** Edit date (unix timestamp) */
    Date: number;
    constructor(data: string | object);
}
