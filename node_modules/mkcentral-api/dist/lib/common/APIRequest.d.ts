/// <reference types="node" />
import { URL } from 'node:url';
/**
 * GET over HTTPS and return the body as text.
 * @param uri HTTPS URL.
 * @returns Response body.
 */
export declare function fetchHttpsContent(uri: string | URL): Promise<string>;
/**
 * GET over HTTPS and return the body as a Buffer.
 * @param uri HTTPS URL.
 * @returns Response body as Buffer.
 */
export declare function fetchHttpsBuffer(uri: string | URL): Promise<Buffer>;
