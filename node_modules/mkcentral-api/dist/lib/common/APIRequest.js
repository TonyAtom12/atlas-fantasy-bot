'use strict';
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.fetchHttpsBuffer = exports.fetchHttpsContent = void 0;
var https = __importStar(require("node:https"));
var InvalidURLError = /** @class */ (function (_super) {
    __extends(InvalidURLError, _super);
    function InvalidURLError(message) {
        var _this = _super.call(this, message) || this;
        _this.name = 'InvalidURLError';
        return _this;
    }
    return InvalidURLError;
}(Error));
var NetworkError = /** @class */ (function (_super) {
    __extends(NetworkError, _super);
    function NetworkError(message) {
        var _this = _super.call(this, message) || this;
        _this.name = 'NetworkError';
        return _this;
    }
    return NetworkError;
}(Error));
var HTTPError = /** @class */ (function (_super) {
    __extends(HTTPError, _super);
    function HTTPError(statusCode, message) {
        var _this = _super.call(this, message) || this;
        _this.name = 'HTTPError';
        _this.statusCode = statusCode;
        return _this;
    }
    return HTTPError;
}(Error));
/**
 * GET over HTTPS and return the body as text.
 * @param uri HTTPS URL.
 * @returns Response body.
 */
function fetchHttpsContent(uri) {
    return new Promise(function (resolve, reject) {
        var urlStr = uri.toString();
        // HTTPS only
        if (!urlStr.startsWith('https://')) {
            return reject(new InvalidURLError("Only https protocol is supported. Received: ".concat(urlStr)));
        }
        var req = https.get(uri, function (response) {
            var statusCode = response.statusCode, statusMessage = response.statusMessage;
            var data = '';
            response.on('data', function (chunk) {
                data += chunk;
            });
            response.on('end', function () {
                if (statusCode && statusCode >= 200 && statusCode < 300) {
                    return resolve(data);
                }
                // Non-2xx -> HTTPError
                reject(new HTTPError(statusCode, "Request failed with status ".concat(statusCode !== null && statusCode !== void 0 ? statusCode : 'unknown').concat(statusMessage ? " ".concat(statusMessage) : '') + (data ? ": ".concat(data) : '')));
            });
            response.on('error', function (err) {
                reject(new NetworkError("Response error: ".concat(err.message)));
            });
        });
        // Timeout: 15s
        req.setTimeout(15000, function () {
            req.destroy(new Error('Request timed out'));
        });
        req.on('error', function (err) {
            reject(new NetworkError("Network error while requesting ".concat(urlStr, ": ").concat(err.message)));
        });
    });
}
exports.fetchHttpsContent = fetchHttpsContent;
/**
 * GET over HTTPS and return the body as a Buffer.
 * @param uri HTTPS URL.
 * @returns Response body as Buffer.
 */
function fetchHttpsBuffer(uri) {
    return new Promise(function (resolve, reject) {
        var urlStr = uri.toString();
        // HTTPS only
        if (!urlStr.startsWith('https://')) {
            return reject(new InvalidURLError("Only https protocol is supported. Received: ".concat(urlStr)));
        }
        var req = https.get(uri, function (response) {
            var statusCode = response.statusCode, statusMessage = response.statusMessage;
            var chunks = [];
            response.on('data', function (chunk) {
                chunks.push(Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk));
            });
            response.on('end', function () {
                var buf = Buffer.concat(chunks);
                if (statusCode && statusCode >= 200 && statusCode < 300) {
                    return resolve(buf);
                }
                // Non-2xx -> HTTPError
                reject(new HTTPError(statusCode, "Request failed with status ".concat(statusCode !== null && statusCode !== void 0 ? statusCode : 'unknown').concat(statusMessage ? " ".concat(statusMessage) : '') + (buf.length ? ": ".concat(buf.toString('utf8')) : '')));
            });
            response.on('error', function (err) {
                reject(new NetworkError("Response error: ".concat(err.message)));
            });
        });
        // Timeout: 15s
        req.setTimeout(15000, function () {
            req.destroy(new Error('Request timed out'));
        });
        req.on('error', function (err) {
            reject(new NetworkError("Network error while requesting ".concat(urlStr, ": ").concat(err.message)));
        });
    });
}
exports.fetchHttpsBuffer = fetchHttpsBuffer;
