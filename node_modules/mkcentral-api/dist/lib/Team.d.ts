import { Roster } from './common/Roster';
import { Manager } from './team/Manager';
import { Approval } from './team/Approval';
import { Language } from './common/Language';
import { Game } from './common/Game';
import { GameMode } from './common/GameMode';
export declare enum TeamDataKind {
    Full = "full",
    List = "list"
}
export declare class Team {
    /** Unique team ID */
    Id: number | null;
    /** Team name */
    Name: string | null;
    /** Team tag */
    Tag: string | null;
    /** Approval status */
    ApprovalStatus: Approval | null;
    /** Team color */
    Color: number | null;
    /** Creation date (unix timestamp) */
    CreationDate: number | null;
    /** Team description */
    Description: string | null;
    /** Historical flag */
    IsHistorical: boolean | null;
    /** Main language */
    Language: string | null;
    /** Logo URL (normalized to https absolute if possible) */
    Logo: string | null;
    /** Managers */
    Managers: Manager[];
    /** Rosters with players and invites */
    Rosters: Roster[];
    /** Indicates which data shape was parsed */
    DataKind: TeamDataKind;
    /** Convenience flag mirroring Player style */
    IsFull: boolean;
    constructor(data: string | object);
    private static normalizeLogo;
    /** Fetch a team by ID and return a populated Team instance. */
    static Get(teamId: number): Promise<Team>;
    /** Load this team's full profile into the current instance (no object replacement). */
    Load(): Promise<void>;
    static readonly Manager: typeof Manager;
    static readonly Approval: typeof Approval;
    static readonly Roster: typeof Roster;
    static readonly Language: typeof Language;
    static readonly Game: typeof Game;
    static readonly GameMode: typeof GameMode;
}
