import { Team } from './Team';
export declare class Player {
    constructor(playerData: string | number | any);
    /**
     * Registry id for the corresponding player
     */
    Id: number;
    /**
     * Forum id for the corresponding player
     */
    UserId: number;
    /**
     * The date on which the user has registered in the mkc registry
     */
    RegisteredDate: Date;
    /**
     * A human-readable string for the registration date
     */
    RegisteredDateHuman: string;
    /**
     * The date on which the user joined their team
     *
     * Only available with category filter in Players collection
     */
    TeamRegisteredDate: Date | null;
    /**
     * A human-readable string for the team joining date
     *
     * Only available with category filter in Players collection
     */
    TeamRegisteredDateHuman: string | null;
    /**
     * The users name in the registry
     */
    Name: string;
    Status: string | null;
    IsBanned?: boolean | null;
    BanReason?: string | null;
    IsHidden?: boolean | null;
    CountryCode: string;
    CountryName: string;
    Region?: string | null;
    City?: string | null;
    DiscordPrivacy?: string | null;
    DiscordTag?: string | null;
    SwitchFC: string | null;
    NNID: string | null;
    FC3DS: string | null;
    MKTourFC: string | null;
    ProfilePicture?: string | null;
    ProfilePictureBorderColor?: number;
    ProfileMessage?: string;
    IsSupporter?: boolean;
    IsAdministrator?: boolean;
    IsModerator?: boolean;
    IsGlobalEventAdmin?: boolean;
    IsGlobalEventMod?: boolean;
    IsEventAdmin?: boolean;
    IsEventMod?: boolean;
    /**
     * Only available with category filter in Players collection
     */
    CurrentTeam?: Player.CurrentTeam;
    /**
     * Only available when directly loading the player
     */
    CurrentTeams?: Player.CurrentTeam[];
    Load(): Promise<void>;
    /**
     * Sends a request to the API asynchronously with the given parameters and returns the result as a new Team object
     * @param {number} playerId
     *
     * Examples:
     * ```js
     * let result = await Team.Get(10);
     * ```
     */
    static Get(playerId: number): Promise<Player>;
}
export declare namespace Player {
    class CurrentTeam {
        constructor(teamData: any);
        Mode?: string;
        ModeTitle?: string;
        ModeKey?: string;
        TeamId: number;
        TeamName: string;
        TeamTag: string;
        TeamStatus: string;
        TeamColor?: number;
        GetTeam(): Promise<Team>;
    }
}
